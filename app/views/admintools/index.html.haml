%h1 Admin tools
%ul
  %li= link_to t('admin_pages.titles.users'), users_path
  %li= link_to t('admin_pages.titles.search_history'), searches_path
%p Clear all data
= button_to t('admin_pages.admin_tools.clear'), clear_admintools_path, method: :post, class: 'btn btn-primary'
%br
%p Upload data
/ <haml:loud> button_to &amp;#39;Upload rows&amp;#39;, admintool_addrows_path, method: :post </haml:loud>
= form_tag({action: :upload}, multipart: true) do
  #{file_field_tag 'ntriplet'}
  %br
  #{submit_tag t('admin_pages.admin_tools.upload'), class: 'btn btn-primary'}
%br
= button_to t('admin_pages.admin_tools.download_rows'), download_admintools_path, method: :get, class: 'btn btn-primary'
%h1 Not fully working yet
%p Upload YAML data
= button_to 'Upload rows', addrows_admintools_path, method: :post, class: 'btn btn-primary'
= form_tag({action: :upload_yaml}, multipart: true) do
  %br
  #{file_field_tag 'yaml_file'}
  %br
  #{submit_tag "Upload", class: 'btn btn-primary'}
%br= button_to 'Download full YAML rows', download_full_admintools_path, method: :get, class: 'btn btn-primary'
